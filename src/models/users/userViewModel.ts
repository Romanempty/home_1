export type UserViewModel = {
    id: string,
    login: string,
    email: string,
    createdAt: string
}

export type userInputModel = {
    login: string,
    password: string,
    email: string
}